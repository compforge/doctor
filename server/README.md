# Doctor Server

This directory is reserved for the optional Doctor server. The current CLI does not require a
server and remains fully usable on its own.

A future TypeScript implementation will reuse `@compforge/doctor-agent` behind a server-specific
interface. Its concrete runtime responsibilities will be defined when that work starts.
