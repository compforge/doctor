# Doctor Server

This directory is reserved for the optional Doctor server. The current CLI does not require a
server and remains fully usable on its own.

A future implementation is planned around the TypeScript version of pi agent. Its concrete
responsibilities and integration boundary will be defined when that work starts.
