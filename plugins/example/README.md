# Example Doctor Plugin

This package is the smallest product-neutral example of a Doctor Plugin. It declares two example
services and a few capabilities without connecting to a real system.

Use it as a starting point for a separately distributed Plugin. Product service names, topology,
queries and access implementations belong in that Plugin rather than in the Doctor CLI.
